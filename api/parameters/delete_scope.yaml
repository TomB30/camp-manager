name: deleteScope
in: query
required: false
schema:
  type: string
  enum: [single, future, all]
  default: single
description: Scope of deletion for recurring events (single=this event only, future=this and future events, all=entire series)

