type: object
required:
  - role
  - scopeType
properties:
  role:
    type: string
    enum: 
      - admin
      - program-admin
      - viewer
  scopeType:
    $ref: "./ScopeType.yaml"
  scopeId:
    type: string
    nullable: true
    description: The unique identifier of the tenant or camp according to the scope type (null for system)

