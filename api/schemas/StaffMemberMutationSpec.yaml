type: object
required:
  - birthday
  - gender
  - roleId
properties:
  birthday:
    $ref: "../fields/Birthday.yaml"
  gender:
    $ref: "../fields/Gender.yaml"
  roleId:
    type: string
    format: uuid
    description: ID of the role this staff member has
  phone:
    type: string
  certificationIds:
    type: array
    items:
      type: string
      format: uuid
    description: IDs of certifications this staff member holds
  groupIds:
    type: array
    items:
      type: string
      format: uuid
    description: IDs of the groups this staff member belongs to (max one housing group allowed)

