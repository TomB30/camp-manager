type: object
required:
  - meta
  - spec
properties:
  meta:
    $ref: "./EntityCreationRequestMeta.yaml"
  spec:
    type: object
    properties:
      type:
        type: string
        enum: [indoor, outdoor, facility, field, water, other]
      capacity:
        type: integer
        minimum: 1
      equipment:
        type: array
        items:
          type: string
      notes:
        type: string
