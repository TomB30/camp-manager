type: object
required:
  - name
  - hexValue
properties:
  name:
    type: string
  hexValue:
    type: string
    description: Hex color value (e.g., "#FF5733")
  default:
    type: boolean
    description: Whether this is the default color for events
