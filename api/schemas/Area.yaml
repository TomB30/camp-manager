type: object
required:
  - meta
  - spec
properties:
  meta:
    $ref: "./EntityMeta.yaml"
  spec:
    type: object
    properties:
      capacity:
        type: integer
        minimum: 1
      equipment:
        type: array
        items:
          type: string
      notes:
        type: string
