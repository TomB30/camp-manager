get:
  summary: List all sleeping rooms
  responses:
    '200':
      description: Success
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../schemas/HousingRoom.yaml'
post:
  summary: Create a new sleeping room
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../schemas/HousingRoom.yaml'
  responses:
    '201':
      description: Created

