get:
  summary: List all rooms
  responses:
    '200':
      description: Success
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../schemas/Location.yaml'
post:
  summary: Create a new room
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../schemas/Location.yaml'
  responses:
    '201':
      description: Created

