parameters:
  - $ref: "../parameters/id.yaml"
get:
  summary: Get housing room by ID
  responses:
    "200":
      description: Success
      content:
        application/json:
          schema:
            $ref: "../schemas/HousingRoom.yaml"
put:
  summary: Update housing room
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "../schemas/HousingRoomUpdateRequest.yaml"
  responses:
    "200":
      description: Success
      content:
        application/json:
          schema:
            $ref: "../schemas/HousingRoom.yaml"
delete:
  summary: Delete housing room by ID
  responses:
    "204":
      description: Deleted
