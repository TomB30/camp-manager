parameters:
  - $ref: "../parameters/id.yaml"
get:
  summary: Get group by ID
  operationId: getGroupById
  responses:
    "200":
      description: Success
      content:
        application/json:
          schema:
            $ref: "../schemas/Group.yaml"
put:
  summary: Update group by ID
  operationId: updateGroupById
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "../schemas/GroupUpdateRequest.yaml"
  responses:
    "200":
      description: Success
      content:
        application/json:
          schema:
            $ref: "../schemas/Group.yaml"
delete:
  summary: Delete group by ID
  operationId: deleteGroupById
  responses:
    "204":
      description: Deleted
